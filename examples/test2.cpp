#include <stdio.h>

int main() {
    // int a = 10, b = 3, c;
    // float x = 2.0F, y;

    // y = x + a;
    // printf("y = %f\n", y);
    // c = a / b;
    // printf("c = %d\n", c);
    // c = a % b;
    // printf("c = %d\n", c);
    // y = a / b;
    // printf("y = %f\n", y);
    // c = x / y;
    // printf("c = %d\n", c);

    int a = 0, c = 0;
    scanf("%d", &a);
    c = getchar();
    printf("%d %c\n", a, c);

    return 0;
}